'use client'

import { IParams } from '@/types/props'
import { use } from 'react'

export default function UploadPage(props: IParams<'id'>) {
  const params = use(props.params)

  return <>
  
  </>
}
